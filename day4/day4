📝 README.md
# 📌 Week 11 - Day 4  
## 🧠 Problem: Construct Binary Tree from Preorder and Inorder Traversal (LeetCode 105)

---

### 🔗 Problem Link
https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/

---

## 📖 Problem Statement

Given two integer arrays:
- preorder (Root → Left → Right)
- inorder (Left → Root → Right)

Construct and return the binary tree.

---

## 🧩 Example

Input:
preorder = [3,9,20,15,7]  
inorder  = [9,3,15,20,7]

Output:
        3
       / \
      9   20
         /  \
        15   7

---

## 🚀 Approach

### 🔹 Key Idea

- First element of preorder = Root
- Find root in inorder → divide tree into:
  - Left subtree
  - Right subtree

---

### 🔹 Steps

1. Take element from preorder (use index)
2. Find it in inorder array
3. Elements left → left subtree  
4. Elements right → right subtree  
5. Recursively build tree  

---

## ⏱️ Time Complexity
O(N²) (can be optimized to O(N) using hashmap)

## 🧠 Space Complexity
O(N) (recursion stack + tree)

---

## 💻 Code (C)
          class Solution {
public:

    int search(vector<int> &inorder,int left,int right,int val){
    for(int i=left;i<=right;i++){
        if(inorder[i]==val) return i;
    }
    return -1;
    }

    TreeNode* helper(vector<int>& preorder, vector<int>& inorder,int &preidx,int left,int right){
        if(left>right) return NULL;
        TreeNode* root = new TreeNode(preorder[preidx]);
        int inidx = search(inorder,left,right,preorder[preidx]);
        preidx++;

        root->left = helper(preorder,inorder,preidx,left,inidx-1);
        root->right = helper(preorder,inorder,preidx,inidx+1,right);

        return root;

    }
    TreeNode* buildTree(vector<int>& preorder, vector<int>& inorder) {
     int preidx=0;
   return helper(preorder,inorder,preidx,0,inorder.size()-1); 
    }
};

```c
